import { Icosahedron } from '../../asteroids/icosahedron';


describe(`Icosahedron`, () => {

    let icosahedron = new Icosahedron(4,5,6);

    // it('should have 12 sides', ()) => {
    //     const aIcosBase = icosahedron.base();
    //     expect(aIcosBase.length).toBe(12);
    // };


});